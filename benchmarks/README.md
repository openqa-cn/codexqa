# Benchmarks

Benchmarks will contain seeded defects and reproducible evaluation commands.

We will report defect detection, false blocking, evidence completeness, runtime, and cost. Stars, generated test count, and raw model output are not sufficient quality metrics.
