# PR / diff review prompt (default)

Produce a risk-driven, graph-evidence code review for this PR/branch vs `diff_base`.

## Role

Senior reviewer for **any** language / polyglot system. Prefer relational graph facts over patch-only opinions. Critique code, not people. Do not rubber-stamp LGTM.

## CodexQA-only gate (all languages)

Before any finding:

1. Evidence pack must exist and pass `validate-evidence.sh --mode pr`.
2. `manifest.engine` must be `codexqa` (or legacy pack with `codexqa` in `commands` / `commands.log`).
3. If CodexQA pack is missing → **blocked** (`missing_codexqa_engine`). Do **not** fall back to git-diff-only, grep-only, or language-native SAST as the primary review.

`lang_stats` + `09-language-profile.json` identify the **primary** and change-focus languages;
still use the same CodexQA pack for all of them.

## Inputs (required)

1. Evidence pack directory with valid `manifest.json` (`mode: pr`) after `validate-evidence.sh --mode pr`
2. `09-language-profile.json` (or legacy WARN + inferred hint from summary)
3. Artifacts listed in the manifest. When `26-review-digest.json` has `dimensions`, that card replaces opening each dimension signal file for the verdict. Symbol diffs, impact edges, and `24-coverage-ledger.json` are still read for sampled symbols.
4. Optional: business goal, team norms, known incidents

If validation failed or gates missing → only blocked template from SKILL.md.

## Process

0. **Model brief, when `31-model-brief.json` exists.** Read that file once, write `judgment.json`, and run `render-review-html.sh`. Do not open `29-judgment-packet.json`, the seed, `01`–`30`, `seal-conclusion.py`, or steps 1–14. The brief already closed report rows, seed hits, and one-line getters. **Judgment packet, when `31` is absent and `29-judgment-packet.json` exists.** Read that file and stop opening the pack. It already has language, stub count, history counts, dimension cards, non-source one-line samples, risk-tier drivers, `report.pr_delta`, suspect packets, and `read_groups[].text` from each group's first path, or `read_groups[].slices` when the file is longer than the inline cap. Do not open `01`–`28`, `diffs/`, `impact/`, signal JSON, or git. Do not open `read_groups[].paths` after the first, and do not open a source file whose text is already in the packet. `report.pr_delta` rows are closed at render, one card per rule and file. Do not copy them into `judgment.json`. `identical_to_base` matches the current base tip and is not a defect. Judge every suspect packet. A confirmed `magic_number` is a convention, not a P0/P1/P2 finding. A `decision_literal` is a finding only when it sets a fee, timeout, or account limit. When `plan_required` is false, do not write a coverage checklist before findings. `plan_required` does not ask for a defect on every file. Ask the open question only when the tier is T0 from an auth, pay, migration, or IaC path. A keyword hit in csv, markdown, or txt is not that path. When that field is absent, keep the checklist. A dimension with `signals_thin`, `suspect_count` 0, and no report row for that dimension gets no dimension essay. A dimension with a suspect or a report row is unchanged. `read_groups[].truncated` true is the whole read. `independent` groups are not ordered by the registry, and the rule list is not repeated for them. `neighbor_groups` are the caller texts. `report.branch_drift` is cited by `seal-conclusion.py` at render; do not harvest those line numbers. Do not hand-copy `coverage_closure`, `line_skips`, or the untested symbol list from `30-conclusion-skeleton.json`. Judge this packet in one pass and skip steps 1–14. Do not open those steps, the channel prompts, `references/business-rule-records.md`, or `seal-conclusion.py`. `rules` is the applicable business-rule text. `skip_notes` closes the other ids. Continue at step 15. Do not hand-write dimension essays or Mermaid. `seal-conclusion.py` fills empty `risk_tier`, `design_fit`, `complexity`, `dependencies`, `resilience`, `privacy`, `rollout`, `performance`, and `llm_judgment` from the signal files at render, so those sections still appear. When `judgment-work/group-*.json` has two or more files, read `shared.json` once and each group once. Source text is only in the group file. Do not read that text again from this packet. Judge those groups concurrently into `judgment-groups/`. Steps 1–14 below apply only when `29-judgment-packet.json` is absent.

1. Read `manifest.json` (`engine`, `primary_language`, `review_language_focus`, `index_quality`, `lang_stats`) and `09-language-profile.json` — lock primary / focus language before findings. If Semgrep, Bandit, gosec, gitleaks, osv-scanner, ruff, or eslint is missing, run `scripts/lib/install-sast-tools.sh` (see SKILL.md preflight) before trusting `23-sast-signals.json`.
1b. **Review digest before any git or signal-file sweep:** when `26-review-digest.json` exists, read that file only. Take `counts`, `history.commits_behind`, `history.commits_ahead`, `history.three_dot_counts`, `history.two_dot_counts`, `report.rows`, `dimensions`, and `non_source_changed` from it. Do not recompute the split with git. Do not open `10-design-fit-signals.json` through `23-sast-signals.json` to decide a dimension verdict; `dimensions` already has `signals_thin`, array counts, risk tier, and tool status. `report.rows` are the `disposition: report` lines, including a short snippet. Open a signal file only when `dimensions.<name>.suspect_count` is above zero and you are on the suspect pass that needs `derive_suspects` slices. The suspect pass reads `27-suspect-queue.json` instead of those signal files. Full path lists live in `26-review-digest-detail.json`; open it only to check one path. `two_dot_not_in_pr` drift is not a PR deletion. CodexQA `diffs/*.diff.json` stays the authority for a sampled symbol body. A legacy digest that still inlines `history.three_dot` arrays may be read as-is; do not also run git.
2. Read `01-stats.json`, `02-summary.json` — note index quality and raw `lang_stats`.
3. Rank work via `03-change-groups.json` (max fan-in first). If empty → blocked / re-index.
3b. **Step 0 — Risk tier first (pre-pipeline):** use `dimensions.risk_tier` (`tier`, `evidence_floor`) when that card is present. Otherwise read `20-risk-tier.json`. Apply [references/dimensions/risk-tier.md](../references/dimensions/risk-tier.md). Lock `tier` / `review_depth.evidence_floor` into conclusion `risk_tier` **before** design-fit. **T0** (auth/pay/migration/IaC): `full_checklist` — all registry dimensions, graph edges-in/reach required, ≥2 evidence cites per finding, paired review recommended; never downgrade for small diffs. **T1**: `trimmed_checklist` — all dimensions; callers/entries required; minor polish skips OK. **T2**: `spot_check` — more explicit `None` OK; deep-read one non-highlighted file; fail → escalate. **T3**: `sample_ci` — sample + pack green; any auth/pay/migration/IaC path → **re-tier T0**. Highest-wins; ratchet up only.
4. For top groups: read `28-symbol-bundle.json` `impacts[].diff` as the sampled symbol-diff text. Open `diffs/<id>.diff.json` only when that impact is missing or `diff_truncated` is true. `review_scope: branch_drift` means the diff is against the base tip, not a PR-commit edit.
5. Callers and test edges for those symbols are `impacts[].callers` and `impacts[].tests_edge_count`. Do not open `impact/*/edges-in.json`, `reach-in.json`, or `tests-reach.json` unless the bundle has no impact for that symbol.
6. Entry risk: use callers in the bundle, then `07-tags.json` if you still need an entry tag. An empty caller list means that symbol was outside the sampled set or has no recorded external caller. It does not mean the method has no caller. Read `25-unit-calls.json` only when the bundle has no caller for a symbol you are citing.
7. Pending closure rows, `read_groups`, and `rule_plan` routing are `28-symbol-bundle.json` `pending` and `skip_notes`. Do not open `24-coverage-ledger.json` unless the bundle is absent. Do not copy `span_hash` into `coverage_closure`; `seal-conclusion.py` fills it at render. Check `08-hot-but-thin.json` only when the bundle's `tests_edge_count` is missing for a symbol you must cite. A passing test that locks the bug in is still a test gap (family F5): the assertion requires an unsafe value, the input stays far from the boundary (including null, zero, negative, and an equal value with a different scale), the batch is empty, threads are not joined, the assertion only counts attempts, a flag’s other value is never run, a condition cannot be true, the test reads private production state or is nested in the production type, or a numeric literal is the pass condition. Those literals are not fixtures. This step and `weak_perf_tests` are the only places for that test-oracle check. Do not repeat it in the business-logic pass. Prefer caller edges in the bundle; `from_count` is NOT fan-in. Under `stubs>=20`, keep blast-radius confidence at `UNKNOWN`.
7b. When `impact/*/edges-in` is empty for high-severity symbols, check `19-annotation-edges.json` for Spring/Resilience4j synthetic callers (`fallbackMethod`, `@GetMapping`, etc.) before citing callers as UNKNOWN-only.
   For hot-but-thin names matching `equals` / `hashCode` / `compare*` / `compareTo` / `cmp`: **open the matching `diffs/<id>.diff.json` and review the body** per [references/dimensions/correctness-family-checks.md](../references/dimensions/correctness-family-checks.md) — do not judge by name alone.
8. Check `06-sensitive-hits.json` (search + `symbols --name` + edges-in) and escalate hits that intersect changed symbols or callers evidenced by **edges-in** (not stub `from_count` alone). If new auth/pay/secret hits contradict a T2/T3 lock, **re-tier upward**.
8c. When `29-judgment-packet.json` exists, skip steps 9–14. Report rows, dimension notes, and call chains are filled at render. Continue at step 12b's one packet pass and step 15.
9. **Design fit first:** use `dimensions.design_fit` (`layer_summary`, `sprawl`, counts). Do not open `10-design-fit-signals.json` unless `suspect_count` is above zero. Apply [references/dimensions/design-fit.md](../references/dimensions/design-fit.md) as **four subsections** (Belong / Layer / Over-engineering / Timing).
10. **Complexity next:** use `dimensions.complexity` (`hot_methods`, counts). Do not open `11-complexity-signals.json` unless `suspect_count` is above zero. Apply [references/dimensions/complexity.md](../references/dimensions/complexity.md).
11. **Dependencies next:** use `dimensions.dependencies` (counts, `signals_thin`). Do not open `12-dependency-signals.json` unless `suspect_count` is above zero. Apply [references/dimensions/dependencies.md](../references/dimensions/dependencies.md). Never invent CVEs. **If changed symbols implement supply-chain / SCA / lock / license checkers**, also apply that card’s **When reviewing supply-chain checker / rule-engine code** section (body vs soft thresholds) — do not rely on Complexity hotspots alone.
12. Continue registry order from `dimensions` (`resilience`, `privacy`, `rollout`, `observability`, `contract`, `maintainability`, `performance`, `sast`, `risk_tier`) and from `report.rows`. Do not open those signal files for the verdict. **Correctness**, then **Resilience:** apply [references/dimensions/resilience.md](../references/dimensions/resilience.md) (timeout / retry / swallow / degrade / partial fail / idempotency — never invent SLO/chaos conclusions; do not re-litigate logic bugs as resilience). **Hard gate:** non-empty counts for `silent_swallows` / `timeout_gaps` / `retry_risks` / `partial_failure_gaps` / `idempotency_gaps` / `exception_unwraps` must become visible findings with `path:line` taken from `report.rows` (family F2 in [references/rule-construction.md](../references/rule-construction.md); do not defer those rows to `residual_risks`) — never Resilience `None` while those counts are above zero. `exception_unwraps` is `ERR-001`. `resource_leaks` is `RES-001` on Correctness. If `residual_hardening` count is above zero, report as residual (not automatic P1) even when findings are None. Then security → **Privacy:** apply [references/dimensions/privacy.md](../references/dimensions/privacy.md). Never invent GDPR/compliance conclusions; do not re-litigate auth/token/pay. Then contract → **Change / rollout:** apply [references/dimensions/rollout.md](../references/dimensions/rollout.md) (migration / dual-write / flags / compat window / breaking announce / rollback — never invent canary/ops conclusions; do not re-litigate caller signature drift as rollout; **Javadoc/comments naming remedy keywords do not waive gaps**). `error_payload_candidates` and `opaque_status_candidates` are semantic rows for the order-16 pass (hit or skip in `semantic_coverage`); they are not hard gates and do not change `signals_thin`. If `residual_rollout` count is above zero, report as residual (not automatic P1). Then concurrency → regression → test gaps → observability → maintainability → **Performance:** apply [references/dimensions/performance.md](../references/dimensions/performance.md) (hot path / N+1 / unbounded allocation — never invent profiler/SLO/p99; do not re-litigate Complexity nesting or Resilience timeout/retry as performance). **Hard gate:** (`21-performance-signals.json`) non-empty `n_plus_one_risks` / `hot_path_risks` / `unbounded_allocation` counts must become findings or explicit deferred residuals with `path:line` from `report.rows` — never Performance `None` while those counts are above zero. Non-empty `weak_perf_tests` must become a `test_gaps` finding. Non-empty `env_config_gaps` must become a rollout finding. Non-empty `uncontrolled_log_sinks` must become an observability finding. If `residual_performance` count is above zero, report as residual (not automatic P1). Apply [references/dimensions/correctness-family-checks.md](../references/dimensions/correctness-family-checks.md) during correctness/concurrency. Depth of each dimension must meet the locked `evidence_floor` from step 3b. `dimensions.risk_tier` supplies `tier` and `evidence_floor`; open `20-risk-tier.json` only when that card is absent.
12a. **Detection rules and SAST:** while on Correctness, Resilience, Security, Design fit, Contract, Concurrency, and Performance, file derive hard-gate rows and SAST `disposition: report` rows. Do not judge `BIZ-004`, `BIZ-005`, `PAY-007`, `SEC-001`, `AUTH-001`, `ARCH-001`, `API-001`, or the other ids listed in [prompts/business-logic-pass.md](business-logic-pass.md) in this step. `PERF-001` is the `n_plus_one_risks` hard gate, not that pass. Construct and extend rules only via [references/rule-construction.md](../references/rule-construction.md). The index is [references/dimension-registry.md](../references/dimension-registry.md). One hit does not close a `rule_id` inside its own channel. File each SAST row with `disposition: report` once. Ignore `drop`. Send only `suspects[]` to the SAST suspect pass. `allow` records a scanner gap and does not rescan the class. In this step, write `rule_coverage` only for SAST classes and for hard-gate ids whose signal rows you filed (`NULL-001`, `RES-001`, `ERR-001`, `PERF-001`). Ids listed in [prompts/business-logic-pass.md](business-logic-pass.md) get `rule_coverage` in that pass. A `hit` must match a finding’s `rule_id` and list every matching line. A business record’s `sast_class` stays on SAST. Pass `--sast-signals` into `merge-llm-findings.py`.
12a1. **Every signal row, not one example.** A rule or hard-gate array is not covered by a single locus. Each row in `resource_leaks` (including `close_not_in_finally`), `charset_gaps`, `null_deref_gaps` (including `unguarded_parse`), `authz_audit_gaps`, `unpooled_connections`, `unused_accumulators`, `env_config_gaps`, `uncontrolled_log_sinks`, `disabled_bounds`, `retry_side_effects`, `shared_mutables`, `process_defaults`, `test_oracle_hits`, and `prod_test_coupling` is a finding only when `disposition` is `report`. A disabling literal (`disabled_bounds`) is not a residual. A max retry count does not close `retry_side_effects`. `test_oracle_inventory` needs one `test_oracle_coverage` row per test method (`hit` or `skip`); a missing row fails the review. `decision_literal` rows inside `magic_numbers` stay on the derive-suspect pass, and `bound_read: true` does not drop an inline literal or a disabling sentinel. A missing `disposition` is unstamped: re-run `scripts/lib/derive_triage.py` and do not file or drop the row yet. `error_payload_candidates` and `opaque_status_candidates` are not this step; judge them only in [prompts/semantic-candidate-pass.md](semantic-candidate-pass.md). `env_config_gaps` is the rollout row for a host or URL. An unread bound that still changes a computed result is `BND-001` in the business-logic pass, not this row and not only a cache-growth note. `magic_numbers`, long files, and `eol_imports` go in `conventions`, not the P0/P1/P2 count; one convention item may list every line of that kind. A status code or URL that changes a branch or an account result is its own defect. One card is one failure scenario: merge only the same root cause and the same fix, with the other lines in `also_lines` and `same_fix: true`. `getFxRate()` inflating a credit and `BigDecimal.equals` skipping a limit are two cards. Title is the consequence. `actor`, `input`, `line`, and `outcome` are who, what input, which line, and the resulting account state; `fix` is one separate sentence. `existing_code` is a short snippet that appears in that file's diff. Do not file a claim that one diff line literally contradicts. `unpooled_connections` stays a finding when `signals_thin` is still true; do not merge it into an N+1 card. A different `rule_id` on the same file and line is not a duplicate and must not be skipped. If the current skill emits a key and the pack lacks it, re-run that `derive-*.sh` before treating the shape as absent. A legacy pack that only WARNs on the missing key is still reviewable for the keys it has.
12b. **Agent LLM judgment last (order 16):** after heuristic draft findings exist, follow [prompts/llm-judgment-pass.md](llm-judgment-pass.md) and [references/dimensions/llm-judgment.md](../references/dimensions/llm-judgment.md). When `29-judgment-packet.json` exists, one pass covers the SAST suspect pass, the business-logic pass, the semantic-candidate pass, and the residual read ([prompts/residual-read-pass.md](residual-read-pass.md)); independent groups in `judgment-work/` run concurrently. When that packet is absent, run those passes as separate prompts, then dedupe with `scripts/lib/merge-llm-findings.py`. Scanner hits do not remove a symbol from that queue. Include `llm_judgment` in `dimensions_covered`. Never emit a second finding that restates an existing heuristic issue.
13. Do not draw Mermaid. `seal-conclusion.py` fills `callers` from `neighbor_groups`, and the HTML renders that chain. A legacy pack without a judgment packet may still draw one diagram from real pack nodes per [references/mermaid-evidence.md](../references/mermaid-evidence.md).
14. Emit report per [templates/review-report.md](../templates/review-report.md). Mark `confidence: UNKNOWN` when graph quality is weak. Cite `primary_language` in the header. P0/P1 objects in `review-conclusion.json` must follow the evidence-binding fields in review-dimensions (template mirrors them). Include optional `risk_tier`, optional `design_fit` (prefer `sections`), optional `complexity`, optional `dependencies`, optional `resilience`, optional `privacy`, optional `rollout`, optional `performance`, optional `llm_judgment`, + `dimensions_covered`. **Reader prose:** dimension fields (esp. `complexity.hotspots`), finding `risk`/`evidence`, `summary`, and residual notes must state risks in plain language for teammates who did not open the pack — see review-dimensions **Reader prose**. Fill `regression_tests[]` for humans: `target` = detailed scenario to retest; `evidence` = plain-language basis (not raw artifact/UUID-only strings). See review-dimensions **Regression must-test writing**.
15. **Required HTML deliverable:** write `<OUT_DIR>/judgment.json` for business defects the scanners missed, confirmed `suspect_hits`, and `test_oracle` answers on `test_oracle_hits` only. Each finding uses the card voice in [prompts/llm-judgment-pass.md](llm-judgment-pass.md): `title` is the failure, `risk` is one consequence, `fix` is `把第 N 行旧文本改成新文本`. Scanner cards are filled by `seal-conclusion.py` with that same voice. Leave `p0`/`p1`/`p2` empty of report rows. `seal-conclusion.py` fills in-scope report-row cards, convention cards for confirmed magic numbers, `span_hash`, `test_gaps`, rule shapes, and default oracle skips before render. Also write a short `<OUT_DIR>/review-conclusion.json`: `summary`, `intent`, `scope`, `risk_tier`, dimension verdicts, and `regression_tests`. Do not hand-copy `report.pr_delta` into the finding lists. Then run:

```bash
./scripts/render-review-html.sh --dir <OUT_DIR>
```

The renderer runs the conclusion closure gates and will not write HTML when they fail. `seal-conclusion.py` closes the following before that gate; do not hand-write them. Every `disposition: report` line must sit on a finding. Each test answers `unsafe_pass`, `boundary_missed`, and `branch_uncovered`, and skip requires all three false. When the inventory row lists `questions`, also answer `locks_private`, `locks_dependency`, `threshold_pass`, and `observability_asserted`. One true answer does not skip the others. A `close: per_line` or `visible_absence` row must be cited on a finding or named in `line_skips` with a note. Do not write a dimension field as none while those rows are still open. `protection_gaps` and `retry_without_backoff` are findings on a retry, batch, or money path. Production symbols without an accepted test edge go to `test_gaps.symbols` or `waived_symbols`. The HTML table lists behavior methods only; a static initializer, a type or constructor, a get/set/is accessor, or a private helper is waived and is not its own row. Multi-shape `look_for` text needs one `shapes[]` entry per shape. When `24-coverage-ledger.json` exists, `coverage_closure` marks every pending symbol `reviewed` or `failed`.

Tell the user the path to `REVIEW-REPORT.html`. Do not skip the HTML step when the review completed successfully.

## Rules

- Cite artifact paths and symbol names/ids for P0/P1; include `callers` from **edges-in** when present.
- When edges-in empty, cite `19-annotation-edges.json` synthetic callers (Spring/Resilience4j) before UNKNOWN-only.
- Coverage: only `tested_count` / tests-reach prove test edges.
- If `index_quality` / stats indicate many stubs/collisions, mark confidence low on blast-radius claims.
  When validate WARNs `stubs>=20`, **cap** edge/reach/path findings at `confidence: UNKNOWN`
  (do not claim complete blast radius). Do not use stub-inflated `from_count` as proof of fan-in size;
  still may flag `tested_count==0` using edges-in / entry evidence. Re-index with `--full` if the graph looks stale.
- Stay in change scope; put material caveats into finding risk text or summary — do not emit a dedicated residual HTML section.
- No secrets in examples; use placeholders.
- Do not run wiki/chat unless the user asked.
- Optional linters/SAST notes are secondary; never replace CodexQA graph evidence.

## Minimum coverage

- Primary language + review focus (from language profile) + overall risk (High/Medium/Low)
- Change summary
- Evaluate **Design fit / Complexity / Dependencies / Resilience / Privacy / Rollout / Performance / Agent LLM judgment** into
  `review-conclusion.json` + `dimensions_covered` (findings or `ok`/`none` verdicts).
  **HTML/report surface:** only emit dimension sections with `concern`/`unknown`
  (Design-fit: only issue subsections). Omit clean `ok`/`none` filler from the report —
  `render-review-html.sh` filters the same way.
- **Agent LLM judgment:** when `31-model-brief.json` exists, findings are already in `judgment.json`. Add `test_oracle` only, then render. Judge each open line once, copy `preset` under `oracle`, and judge only `questions`. `boundary_missed` stays false unless `preset` is true. Do not re-judge an id already in `suspect_hits`. If `still_open` is empty, read only methods that own a `test_oracle_open` line. The brief then omits closed shapes, candidate cards, and rules. Do not rewrite findings and do not run `merge-llm-findings.py`. When `31` is absent, follow `prompts/llm-judgment-pass.md`.
- **Dependencies / supply chain** checker PRs: still apply the full **When reviewing
  supply-chain checker** checklist on the dependencies card when that code changes.
- **Risk tier** locked from `20-risk-tier.json` (T0–T3 evidence floor) before design-fit; auth/pay/migration/IaC → T0 deepen (always show when present)
- P0 / P1 / P2 lists (or None)
- Regression must-test: each row is an **executable scenario** (entry/caller + inputs + expected outcome). Evidence column must be **plain-language** (what changed / which method / which callers) — never only pack paths, `source:N`, bare symbol UUIDs, or tags like `hot-but-thin`
- Test gaps (edge-based)
- Sensitive path section (or none)
- Call-chain graphs on findings / concern dimension cards via `call_chain` (pure HTML; see mermaid-evidence.md HTML surface)
- **`review-conclusion.json` + `REVIEW-REPORT.html`** via `scripts/render-review-html.sh`

Do **not** put「建议修复顺序」or「残留风险与假设」in the HTML report.
