# Upload test plan to the case platform (removed)

This Skill does not upload the test plan to a case platform, does not call `/api/xbot/*`, and does not create case subsets.

The test plan is saved only locally at `{run_dir}/testdesign/test_design.md`. Do not write to a doc platform. Do not install or call external doc/identity CLI tools.
