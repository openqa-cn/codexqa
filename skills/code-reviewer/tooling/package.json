{
  "name": "code-reviewer-scripts",
  "version": "2.0.0",
  "description": "Optional code review scripts (dependency scan / bundle analysis / progress tracking)",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test contract-checks.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
